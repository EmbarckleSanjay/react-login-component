import { IENVConfig } from 'types/envConfig';

const settings: IENVConfig = {
  apiBaseURL: 'http://localhost:3000/',
  apiVersionPath: '/login',
  siteUrl: 'http://localhost:3000/',
  mode: 'localdev',
};

export { settings };
